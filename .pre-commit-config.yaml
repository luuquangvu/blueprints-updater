repos:
  - repo: local
    hooks:
      - id: ruff-format
        name: ruff-format
        entry: uv run tools/validate.py uv run ruff format
        language: system
        pass_filenames: false
        always_run: true

      - id: ruff-check
        name: ruff-check
        entry: uv run tools/validate.py uv run ruff check --fix
        language: system
        pass_filenames: false
        always_run: true

      - id: ty-check
        name: ty-check
        entry: uv run tools/validate.py uv run ty check
        language: system
        pass_filenames: false
        always_run: true

      - id: pyright
        name: pyright
        entry: uv run tools/validate.py uv run pyright
        language: system
        pass_filenames: false
        always_run: true

      - id: interrogate
        name: interrogate
        entry: uv run tools/validate.py uv run interrogate
        language: system
        pass_filenames: false
        always_run: true

      - id: pytest
        name: pytest
        entry: uv run tools/validate.py uv run pytest
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]

      - id: prettier
        name: prettier
        entry: uv run tools/validate.py npx prettier --write .
        language: system
        pass_filenames: false
        always_run: true
